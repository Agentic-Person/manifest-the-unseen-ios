/**
 * Subscription pages layout.
 * Minimal layout for checkout success/cancel pages.
 */
export default function SubscriptionLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
