# Prevent npm from looking at parent workspace
# This ensures Vercel only installs web dependencies
legacy-peer-deps=true

# CRITICAL: Disable npm workspaces detection
# Without this, npm walks up to root package.json, sees workspaces config,
# and hoists React 19 (from mobile) to root node_modules/
workspaces=false
