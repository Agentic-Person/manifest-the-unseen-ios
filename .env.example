# =============================================================================
# Manifest the Unseen - Environment Variables Template
# =============================================================================
# IMPORTANT: Copy this file to .env and fill in your actual values
# NEVER commit .env to version control - it's in .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Get these from: https://app.supabase.com/project/_/settings/api
# Format: https://[PROJECT_ID].supabase.co

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Service role key should NEVER be exposed in mobile app
# Only use in backend/admin scripts
# The anon key is safe to use in the React Native app

# -----------------------------------------------------------------------------
# AI API Keys
# -----------------------------------------------------------------------------

# Anthropic Claude API (Primary AI monk companion)
# Get from: https://console.anthropic.com/
# Used for: AI wisdom chat (RAG-powered conversations)
# Pricing: ~$3 input, ~$15 output per million tokens
# Estimated cost: ~$0.016 per chat message
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# OpenAI API (Embeddings + GPT-4 fallback)
# Get from: https://platform.openai.com/api-keys
# Used for:
#   - Text embeddings for RAG (text-embedding-3-small)
#   - Optional: Whisper transcription (we use on-device primarily)
#   - GPT-4 fallback for advanced reasoning
# Pricing: Embeddings $0.02/1M tokens, GPT-4 $10 input/$30 output per 1M tokens
OPENAI_API_KEY=sk-your-key-here

# -----------------------------------------------------------------------------
# RevenueCat Configuration (Subscriptions)
# -----------------------------------------------------------------------------
# Get from: https://app.revenuecat.com/
# Used for: Managing subscription tiers (Novice, Awakening, Enlightenment)
# Free tier: Up to $10K MRR
# See: docs/api-keys-guide.md for setup instructions

# Public SDK key (safe to embed in mobile app)
REVENUECAT_API_KEY_IOS=appl_your-ios-key-here
REVENUECAT_API_KEY_ANDROID=goog_your-android-key-here

# REST API Key (backend only - DO NOT use in mobile app)
REVENUECAT_REST_API_KEY=sk_your-rest-api-key-here

# Optional: For cross-platform support
REVENUECAT_APP_ID=app_your-app-id

# -----------------------------------------------------------------------------
# Analytics & Monitoring
# -----------------------------------------------------------------------------

# TelemetryDeck (Privacy-focused analytics)
# Get from: https://dashboard.telemetrydeck.com/
TELEMETRYDECK_APP_ID=your-telemetrydeck-app-id

# Sentry (Error tracking)
# Get from: https://sentry.io/
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# -----------------------------------------------------------------------------
# Apple Developer
# -----------------------------------------------------------------------------

# Apple Sign-In Configuration
# Set up in Apple Developer Console and Supabase Auth settings
APPLE_TEAM_ID=your-apple-team-id
APPLE_SERVICES_ID=com.yourcompany.manifesttheunseen.services
APPLE_KEY_ID=your-apple-key-id

# -----------------------------------------------------------------------------
# Environment Configuration
# -----------------------------------------------------------------------------

# Environment: development | staging | production
NODE_ENV=development

# API Base URL (for future web companion)
API_BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Feature Flags (Optional)
# -----------------------------------------------------------------------------

# Enable/disable features for testing
ENABLE_AI_CHAT=true
ENABLE_VOICE_JOURNALING=true
ENABLE_MEDITATIONS=true
ENABLE_VISION_BOARDS=true
ENABLE_ANALYTICS=false

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------

# Log level: debug | info | warn | error
LOG_LEVEL=debug

# Enable debug mode
DEBUG=true

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. NEVER commit .env file to version control
# 2. Use different API keys for development/staging/production
# 3. Rotate keys regularly (at least every 90 days)
# 4. Use environment-specific Supabase projects
# 5. Keep service_role_key secure - it bypasses RLS!
# 6. Store production keys in secure password manager (1Password, LastPass)
# =============================================================================
