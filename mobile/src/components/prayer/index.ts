/**
 * Prayer Components
 *
 * Components for the prayer player feature with synchronized text display.
 */

export { PrayerTextDisplay, default as PrayerTextDisplayDefault } from './PrayerTextDisplay';
