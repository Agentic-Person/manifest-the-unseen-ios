/**
 * Entry Point for Expo + React Native App
 *
 * This file registers the root component with Expo.
 * Expo handles the native app initialization.
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Register the main App component as the root
registerRootComponent(App);
