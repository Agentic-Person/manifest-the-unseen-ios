# =============================================================================
# Manifest the Unseen - Environment Variables Template
# =============================================================================
# This file serves as a template for environment variables needed for the app.
# Copy this file to .env for local development and fill in your actual values.
#
# IMPORTANT SECURITY NOTES:
# - NEVER commit .env files with real credentials to version control
# - For production builds, use EAS Secrets (see EAS_SECRETS_SETUP.md)
# - The .gitignore file should exclude .env, .env.local, .env.*.local
# =============================================================================

# =============================================================================
# Supabase Configuration
# =============================================================================
# Get from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# Used for: Authentication, database, storage, and all backend services
#
# The ANON key is safe to expose in mobile apps (protected by Row Level Security)
# For local development, you can use cloud Supabase or local instance
# =============================================================================

# Production/Cloud Supabase (default)
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Local Supabase (for development, uncomment to use)
# EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# =============================================================================
# RevenueCat Configuration (Subscriptions)
# =============================================================================
# Get from: https://app.revenuecat.com/ → Project Settings → API Keys
# Used for: Managing subscription tiers (Novice, Awakening, Enlightenment)
#
# Free tier: Up to $10K MRR, then $0.01 per $1 MTR
# These SDK keys are safe to embed in mobile apps
# =============================================================================

# iOS SDK Key (required for iOS builds)
EXPO_PUBLIC_REVENUECAT_IOS_KEY=appl_your_ios_key_here

# Android SDK Key (for future Android support)
# EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=goog_your_android_key_here

# =============================================================================
# AI Service API Keys (Backend Only - NOT EXPO_PUBLIC_)
# =============================================================================
# SECURITY WARNING: These keys should NEVER be exposed to the mobile app
# They should only be used in Supabase Edge Functions or other backend services
# DO NOT prefix with EXPO_PUBLIC_ - these must remain server-side
# =============================================================================

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
# Used for: Text embeddings (RAG), GPT-4 fallback, Whisper transcription (future)
# Cost: ~$0.0001/1K tokens (embeddings), ~$0.03/1K tokens (GPT-4)
OPENAI_API_KEY=sk-proj-your_openai_key_here

# Anthropic Claude API Key
# Get from: https://console.anthropic.com/settings/keys
# Used for: Primary AI wisdom chat companion
# Cost: ~$0.015/1K tokens (Claude 3.5 Sonnet)
ANTHROPIC_API_KEY=sk-ant-api03-your_anthropic_key_here

# =============================================================================
# Analytics & Monitoring (Optional - for future implementation)
# =============================================================================

# TelemetryDeck App ID (Privacy-focused analytics)
# Get from: https://dashboard.telemetrydeck.com/
# EXPO_PUBLIC_TELEMETRYDECK_APP_ID=your_app_id_here

# Sentry DSN (Error tracking and performance monitoring)
# Get from: https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/
# EXPO_PUBLIC_SENTRY_DSN=https://your_sentry_dsn_here

# =============================================================================
# Feature Flags
# =============================================================================
# Use these to enable/disable features during development and testing
# =============================================================================

# Enable AI chat feature (requires ANTHROPIC_API_KEY in backend)
ENABLE_AI_CHAT=true

# Enable voice transcription (requires react-native-whisper setup)
ENABLE_VOICE_TRANSCRIPTION=true

# Enable analytics tracking (requires TelemetryDeck setup)
ENABLE_ANALYTICS=false

# Enable Sentry error tracking
ENABLE_SENTRY=false

# =============================================================================
# Development Settings
# =============================================================================
# These settings control development-specific behavior
# =============================================================================

# Node environment (development, staging, production)
NODE_ENV=development

# Enable debug logging
DEBUG=true

# Skip authentication during development (UI testing)
# WARNING: Set to false for any builds that connect to production Supabase
EXPO_PUBLIC_DEV_SKIP_AUTH=false

# Development password (only used when DEV_SKIP_AUTH=true)
# This is for local UI testing only, not real authentication
EXPO_PUBLIC_DEV_PASSWORD=TestPassword123!

# =============================================================================
# Build-Specific Configuration
# =============================================================================
# These are typically set via EAS Secrets for different build profiles
# Local .env values are used for development only
# =============================================================================

# Build profile (development, preview, production)
# EAS_BUILD_PROFILE=development

# =============================================================================
# Notes for Production Deployment
# =============================================================================
#
# For EAS builds (preview/production), set secrets via EAS CLI:
#
#   eas secret:create --scope project --name EXPO_PUBLIC_SUPABASE_URL --value "https://..."
#   eas secret:create --scope project --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value "eyJ..."
#   eas secret:create --scope project --name EXPO_PUBLIC_REVENUECAT_IOS_KEY --value "appl_..."
#
# Backend API keys (OpenAI, Anthropic) should be set in Supabase Edge Functions:
#   1. Go to Supabase Dashboard → Project Settings → Edge Functions
#   2. Add secrets: OPENAI_API_KEY, ANTHROPIC_API_KEY
#   3. These will be available to your Edge Functions via Deno.env.get()
#
# See EAS_SECRETS_SETUP.md for detailed setup instructions.
# =============================================================================
