{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "npm install --workspace=packages/shared --workspace=web && cd packages/shared && npm run build && cd ../../web && npm run build",
  "outputDirectory": "web/.next",
  "installCommand": "echo 'Using buildCommand for install'",
  "framework": "nextjs"
}
